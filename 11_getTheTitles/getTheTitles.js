const getTheTitles = function(input) {
    let arr = [];
    arr.push(input.forEach(element => {
        return "title";
    }))
    return arr;
};

// Do not edit below this line
module.exports = getTheTitles;
